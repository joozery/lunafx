import { AdminTeamClient } from "@/components/admin/AdminTeamClient";

export const metadata = {
  title: "จัดการผู้ดูแลระบบ (Admin Team) | Lunaforex Admin",
};

export default function AdminTeamPage() {
  return <AdminTeamClient />;
}
